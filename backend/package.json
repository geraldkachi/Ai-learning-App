{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "repository": "",
  "homepage": "",
  "keywords": [
    "AI learning assistant",
    "prediction"
  ],
  "author": "Adimora Augustine Onyekachi",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.ts",
    "start": "node server.ts",
    "test": "jest",
    "test-init": "jest --init",
    "prettier-check": "npx prettier --check .",
    "deploy": "yarn build && yarn start",
    "lint": "eslint --ext .ts --ignore-path .gitignore .",
    "build": "yarn install && tsc -p .",
    "prepare": "husky install",
    "docker-build": "docker build -t trisonet ./",
    "docker": "docker run trisonet",
    "pm2": "pm2 start npm --name \"trisonet_BE\" -- start",
    "restart": " yarn && yarn build && pm2 restart trisonet_BE"
  },
  "dependencies": {
    "-": "^0.0.1",
    "@google/genai": "^1.38.0",
    "D": "^1.0.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-validator": "^7.3.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.5",
    "multer": "^2.0.2",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/multer": "^2.0.0",
    "@types/node": "^25.2.2",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
